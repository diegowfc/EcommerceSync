{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=EcommerceSync;Username=postgres;Password=admin;"
  },
  "AllowedHosts": "*",
  "RabbitMq": {
    "Host": "rabbitmq://localhost", // ou a URL real do seu broker
    "User": "guest", // usuário padrão do RabbitMQ
    "Pass": "guest" // senha padrão do RabbitMQ
  }
}
